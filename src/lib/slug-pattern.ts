/** HTML pattern attribute for URL slugs (hyphen must be escaped for Chrome /v flag). */
export const SLUG_INPUT_PATTERN = "[a-z0-9\\-]+";

/** Optional slug segments (empty allowed). */
export const SLUG_INPUT_PATTERN_OPTIONAL = "[a-z0-9\\-]*";
