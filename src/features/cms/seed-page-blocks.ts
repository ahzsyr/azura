/** Re-exports for Prisma seed scripts — logic lives in page-default-blocks (client-safe). */
export {
  CMS_PAGE_SLUGS,
  buildDefaultPageBlocks,
  buildDefaultPageBlocksFromTemplate,
  isEmptyBlocks,
} from "./page-default-blocks";
