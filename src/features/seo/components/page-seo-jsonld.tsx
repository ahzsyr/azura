/**
 * @deprecated Structured data is rendered globally via StructuredDataGraph in the locale layout.
 * Kept for backward compatibility with any external imports.
 */
export async function PageSeoJsonLd() {
  return null;
}
