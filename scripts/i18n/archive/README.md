# Archived i18n migration scripts

One-off codemods and restore utilities from the EntityTranslation migration (2025–2026). **Not wired in `package.json`.** Do not run on a live codebase unless you know what you are doing.

Active i18n tooling lives in the parent directory (`validate-i18n.mjs`, `backfill-translations.ts`, `verify-parity.ts`, etc.).
