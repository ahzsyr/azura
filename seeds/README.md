# Catalog seed fixtures

Not read at runtime in cloud-native production. Used by `npm run catalog:seed-*`, local filesystem dev mode, and static build-time imports (e.g. currency config baseline, UI copy).

See `seeds/catalog/` for JSON fixtures and `seeds/demo-profiles/` for demo site import profiles.
